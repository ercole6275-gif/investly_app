
name: investly
description: Complete Flutter app (Android + Web)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
